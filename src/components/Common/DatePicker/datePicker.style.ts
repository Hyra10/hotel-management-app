import { createUseStyles } from 'react-jss';

export default createUseStyles({
  inputWrapper: {
    display: 'flex',
    flexDirection: 'column',
    paddingRight: 10,
    paddingLeft: 10,
    paddingBottom: 10,
  },
});